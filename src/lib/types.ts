export type TodoDTO = {
  id: string;
  date: string;
  time: string;
  text: string;
  done: boolean;
  createdAt: string;
};

export type ImageDTO = {
  id: string;
  url: string;
  uploadedAt: string;
};
